anyLogicPluginsDir=/home/erik/anylogic/8.9.7/plugins
# Only needed when the AnyLogic workspace is not at ~/.AnyLogicPLE/Workspace8.8,
# which is where the copyGeneratedJava task looks by default.
#anyLogicWorkspaceDir=/home/erik/.AnyLogicPLE/Workspace8.8

# The interpreter that generates the PV production profiles, which needs the packages in
# core/src/main/resources/pv/requirements.txt. Defaults to `python` on the PATH, which on
# Windows is usually the Microsoft Store stub rather than a real interpreter, so set it there.
# On Windows, for example:
#luxPythonExecutable=C:/Users/luc-s/miniconda3/python.exe
luxPythonExecutable=/home/erik/zenmo/lux-esdl/.venv/bin/python
luxAnyLogicAlpxFiles.1=/home/erik/zenmo/ANYLOGIC/zero_engine/Zero_engine.alpx
luxAnyLogicAlpxFiles.2=/home/erik/zenmo/ANYLOGIC/zero_results_UI/results_UI.alpx
luxAnyLogicAlpxFiles.3=/home/erik/zenmo/ANYLOGIC/zero_Interface-Loader/Zero_Interface-Loader.alpx
